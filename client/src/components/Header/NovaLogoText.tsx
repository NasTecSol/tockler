import { Text } from '@chakra-ui/react';

export const NovaLogoText = (props) => (
    <Text
        fontSize="1.5rem"
        fontWeight="bold"
        color="currentColor"
        letterSpacing="wider"
        {...props}
    >
        NOVA
    </Text>
);
